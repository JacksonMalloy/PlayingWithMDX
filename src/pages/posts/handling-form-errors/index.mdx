---
title: Handling Form Errors
path: /handling-form-errors
date: 2020-10-25
tags: ["Apollo Client", "React", "GraphQL"]
published: false
---

import { SampleField } from './sampleField'

# Handling Form Errors

tsx:title=src/components/field.tsx {6-14}


```tsx:title=src/components/field.tsx
interface FieldProps {
  id: string
  name: string
  type: string
  autoComplete?: string
  required?: boolean
  placeholder?: string
  label?: string
  value: string
  onChange: EventHandler<any>
  onBlur?: FocusEventHandler
  error?: string
  serverError?: any
}

export default function Field(props: FieldProps) {
  const {
    id,
    name,
    type = 'text',
    autoComplete,
    required,
    placeholder = 'Placeholder',
    label = 'First Name',
    value = '',
    onChange,
    onBlur,
    error,
    serverError,
  } = props

  return (
    <StyledField>
      <StyledLabel htmlFor={name}>{label}</StyledLabel>
      <StyledInput
        id={id}
        name={name}
        type={type}
        serverError={serverError?.error}
        autoComplete={autoComplete}
        required={required}
        placeholder={placeholder}
        value={value}
        onChange={onChange}
        onBlur={onBlur}
        error={error}
      />
      {error && <StyledError>{error}</StyledError>}
      {serverError?.error && !error && <StyledError>{serverError?.message}</StyledError>}
    </StyledField>
  )
}

```
<SampleField />