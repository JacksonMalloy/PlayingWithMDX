import styled from 'styled-components'
import Header from '../../components/header'
export const myVariable = 'Yay!'

wtf
this is wack

holy shit 
fromf
fromf
fromf
fromf
asdfafas
df
asdfaasdffda
sdfasdf

# Interesting

## This is an H2

### An H3 here!

#### An H4, and so on...

*Google Analytics* is a web analytics service offered by Google that tracks and reports website traffic. 
They offer a great dashboard and the service is **free**.

React inline styles are not specified as a string. 
They are specified as a javascript object whose key is the camelCased version of the style name, 
and whose value is the style’s value, usually a string, e.g.

*Google Analytics* is a web analytics service offered by Google that tracks and reports website traffic. 
They offer a great dashboard and the service is **free**.


```tsx:title=src/components/code.tsx {7-9}

const Layout = ({ children }) => {
  return (
    <StyledLayout>
      <StyledContainer>
        <MDXProvider components={components}>
          <GlobalStyles />
          {children}
        </MDXProvider>
      </StyledContainer>
    </StyledLayout>
  )
}

export default Layout
```

huh is this going to asdfkasdf

asdfa
sdf
asdfaasdf



asdfasdf